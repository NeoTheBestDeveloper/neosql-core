subdir('db_driver')
subdir('sql_driver')
subdir('utils')
subdir('db_dto')

neosql_core_src = files(
  'connection.c',
)

neosql_core_src += db_src
neosql_core_src += sql_driver_src
neosql_core_src += utils_src
neosql_core_src += db_dto_src
