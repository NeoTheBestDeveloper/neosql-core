subdir('driver')
subdir('sql')
subdir('utils')
subdir('dto')

neosql_core_src = files(
  'connection.c',
)

neosql_core_src += driver_src
neosql_core_src += sql_src
neosql_core_src += utils_src
neosql_core_src += dto_src
